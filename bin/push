#!/bin/sh
echo "Generating index.md..."
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
uv run "$PROJECT_DIR/src/gen_index.py"

echo "✒️ commit and push changes..."
git add .
git commit -m "update"
git push
