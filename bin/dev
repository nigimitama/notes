#!/bin/sh
# run dev server
HTML_PATH="$PROJECT_DIR/book/_build/html"
if [ ! -d "$HTML_PATH" ]; then
  echo "📙building a book..."
  cd "$PROJECT_DIR/book" && uv run jupyter-book build --html
fi
cd "$HTML_PATH" && python3 -m http.server